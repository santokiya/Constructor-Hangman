var inquirer = require('inquirer')
var names = require('./word.js')

var game = {
    //array of words
    wordBank: [],
    guessesLeft: 5,
    //empty field to store the letters guessed
    lettersGuessed: '',

    //function to iterate through the word bank and retrieve a new word
    //maybe a display-hide state 


    //function to start the game and then run the function
    startGame: function() {

    }
}